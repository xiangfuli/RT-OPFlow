#ifndef SYSTEM_DEFS_H
#define SYSTEM_DEFS_H

typedef enum {
  HR_MODE,
  FLOW_MODE
} SystemOperationMode;

typedef enum {
  RUNNING,
  INITIALIZATION_FAILED
} SystemState;

#endif /* SYSTEM_DEFS_H */
